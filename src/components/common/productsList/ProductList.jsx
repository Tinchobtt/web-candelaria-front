const ProductList = ({products, ProductCard}) =>{
    return(
        <>
            {
                // products.map(product => (
                //     <p key={product.id}>{product.title}</p>
                // ))
            }
        </>
    )
}

export default ProductList